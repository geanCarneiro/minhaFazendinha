import { ShortNumberPipePipe } from './short-number-pipe.pipe';

describe('ShortNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ShortNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
